# HTTP(S)

HTTP(S), aka HyperText Transfer Protocol (Secure), is the protocol/set of rules that allows computers to exchange information with one-another via the web. This is the backbone of web data communication, making it essential to understand properly.