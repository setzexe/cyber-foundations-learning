# Powershell

PowerShell is a powerful command-line shell and scripting environment that engrains deep into Windows. This is the modernized version of Command Prompt. Powershell remaind significant because it allows direct access to system configurations, processes/services, and security controls. This makes it used both for legitimate and malicious purposes. 

**This is the command line we will be working with, not the legacy command prompt.**